/**
 * User role constants matching backend UserRole enum.
 */
export const ROLES = {
  CS: 'CS',
  ENGINEER: 'ENGINEER',
  ADMIN: 'ADMIN',
};

/** All role values as array (for dropdowns, filters). */
export const ROLE_LIST = [ROLES.CS, ROLES.ENGINEER, ROLES.ADMIN];
